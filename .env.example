# Domain Traefik routes on (must match the tunnel's public hostname)
SITE_DOMAIN=blog.skraburski.com

# Set by CI at deploy time; only needed for local overrides
# CI_REGISTRY_IMAGE=registry.gitlab.com/you/blog
# IMAGE_TAG=latest
